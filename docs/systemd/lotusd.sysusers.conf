# The system user lotusd runs as. Install to /usr/lib/sysusers.d/lotusd.conf and
# apply with `systemd-sysusers`. Operators who should be able to run lotusctl
# join the lotus group.
u lotus - "iroh-lotus daemon" /var/lib/lotus
