[package]
name = "render"
version.workspace = true
rust-version.workspace = true
license.workspace = true
edition.workspace = true
publish.workspace = true

[features]
# Lets a CLI take a `ColorChoice` straight off the command line. Off by
# default: nothing about rendering needs an argument parser.
clap = ["dep:clap"]

[dependencies]
clap = { workspace = true, optional = true }

# Our crates
wire.workspace = true
